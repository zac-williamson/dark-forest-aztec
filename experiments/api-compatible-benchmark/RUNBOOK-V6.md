# V6 fee run

Prepared offline; deployment requires the coordinator's final genuine native-build release. Use the primary benchmark harness with `/tmp/df-api-compatible-v6-native`. This harness loads exact native artifacts and does not require promoting the isolated V6 production source. The six V6 compatibility suites use the separately frozen V6 test harness; their source snapshot must be recorded explicitly.

```sh
RAYON_NUM_THREADS=2 HARDWARE_CONCURRENCY=2 NODE_BACKEND=js LOG_LEVEL=error node experiments/api-compatible-benchmark/run-plan-v6.mjs --execute all
RAYON_NUM_THREADS=2 HARDWARE_CONCURRENCY=2 NODE_BACKEND=js LOG_LEVEL=error node experiments/api-compatible-benchmark/run-carried-v6.mjs --execute
```

The first command creates `candidate-v6`, binds its new namespaces and workers, and checks all 45 original comparisons. It retains the genuine original 44 cached receipts, creates a new matched `find_v6` pair after setup, and initializes the new player before Find seeds that player. It then runs the original private/public actions, queued moves, initialization and Refresh edge cases, and sponsored Move/Refresh in the established order. The first four must pass fee and exact-effect gates before the remaining cases. Later fee regressions are recorded, not hidden by stopping coverage.

Move's new namespace starts at counter 1 and allocates IDs 2 through 14 in the original order. The carried extension follows only after the frozen V5 carried baseline pairs have succeeded; it reuses those exact original cases and allocates V6 IDs 15 and 16. It verifies every Location callback: Refresh5=5, Move5=10, Refresh20=20, Move20=40. The existing original namespace's live counter is derived from all saved original Move receipts, including the extension. It is never reset or copied into the new candidate. Cached rows are validated against canonical receipts and exact artifacts before reuse. An uncertain submission journal prevents automatic retries.

All action inputs and seeds must match the original pair exactly except the refreshed top-level transaction timestamp. Ordered prospective effects are checked before broadcast; ordered mined effects are checked afterward. Only documented current-action timestamps and block fields are normalized. Every deployment's actual stored root is independently checked against its entire exact event state. Historical and new raw roots are not claimed identical when their timestamps differ. Time-dependent branches must produce the same remaining fields and relative durations, or the candidate is stopped before sending and a newly matched baseline is required.

Fee reports use complete billed transaction gas at the fixed observed Fee Juice price and separately reconcile the actual mined receipt fee. `proverEnabled:false`, no transaction proofs, no native/browser proof profiling, and no AVM diagnostic profiling. All child processes keep both worker limits at two. The original 45-case V5 report and its immutable snapshot remain unchanged. V6 writes distinct deployments, fixtures, rows and coverage outputs; carried cases have their own four-pair coverage report.

V6 candidate actions opt into `BENCH_FINAL_TX_GATE=1`. A supported AztecNode adapter is supplied when creating the wallet. Immediately before the final wallet transaction is forwarded, the adapter runs `simulatePublicCalls(tx, false)` with fee enforcement, checks normalized ordered original game event fields, captures complete billed gas and journals that exact transaction. A rejection or journal failure prevents forwarding. It verifies that transaction bytes and hash were not changed and makes one SDK send invocation with the same object. The SDK transport may retry the identical serialized request; the harness never reconstructs or automatically resubmits a new transaction. Exact stored roots against complete emitted state and exact unchanged seeded roots are checked after mining. The mined hash and actual receipt fee must reconcile to this final transaction. This removes the harness's separate private simulation while retaining the wallet's normal estimate/transaction construction. The new gate never sends a stub-account estimate. V5 and new original Find measurements keep their established path; no proof or timing claim is added.

Offline validation completed: `validate-run-plan-v6.mjs` reconstructed all 44 cached original fixtures exactly, validated all 45 unique operation identities, confirmed only `find_v6` needs a new ordinary baseline, checked initialization actor/order and Move IDs 2..14, and opened no node or wallet.
