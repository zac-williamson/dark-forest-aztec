# Preserved-chain recovery before V4

The original node26340 developed LMDB `mdb_txn_begin:22` errors at15:03:08 after its last successful block278. Metadata RPC remained available, while database-backed calls hung. Read-only L1 inspection proved pending and proven tips were both278 at block1192, so the local attempted unwind to246 was not evidence of actual L1 pruning. The missing local hash caused the SDK walker to reach its local finality floor.

The recovery preserved Anvil8919 on8551, its10.46MB state dump, the original launch configuration, log and preflight under `/tmp/df-api-compatible-recovery-before-v4-20260911`. After graceful shutdown remained stalled, only the verified failed node26340 was terminated. The original `/tmp/df-api-compatible-network52` directory remains untouched. Its39files were copied to `/tmp/df-api-compatible-network52-recovered-v4`.

The replacement node58106 serves8097/8897 from that copy. `resume-owned-node.mjs` calls `createAztecNodeService` directly with the exact existing L1 addresses and genesis. It never invokes local-network deployment or account/FPC setup. It explicitly retains `useAutomineSequencer:true`, `automineEnableProveEpoch:true`, `realProofs:false`, and the test date provider aligned to the last mined L1 timestamp. Synthetic local epoch settlement remains enabled; it generates no cryptographic proof. Both RPC servers bind127.0.0.1, and the saved admin authentication hash is reused.

An initial attempt opened the copied databases successfully but failed in HTTP startup because the repository tsx loader altered a Node25 CommonJS dependency. The replacement plain-JavaScript launcher runs directly through Node, matching the original CLI's loader behavior. Its failed attempt log was preserved separately. No reset, replay, unwind or LMDB error occurred during the successful restart.

`results/baseline-drift-before-v4-block-278.json` records successful live verification of the exact head278, all12 saved baseline-v2/candidate-v2/candidate-v3 mined receipts and their block hashes/fees,17 original class identities,11 latest baseline state roots, and Arrivalcounter2. `results/recovery-preflight-before-v4.json` records the matching genesis, L1 configuration and database versions. The read-only checks open no wallet and send no transactions.

The running log is `/tmp/df-api-compatible-network52-recovered-v4.log`; current execution session is90471. The fee wallet remains `/tmp/df-api-compatible-wallet52-v2`, closed until final build GO. No V4 gameplay fee or proving result is implied by recovery. The one-off launcher intentionally refuses a different L1 head, rollup, genesis or schema; it must not be reused later without another explicit recovery review and fresh recorded preflight.

After final build GO, the first V4 setup transaction exposed a separate resume condition: the SDK automine sequencer initializes its in-memory `lastBuiltSlot` to-1, so it targeted the already-used slot278. The L1 simulator rejected that proposal and the SDK returned its transaction to the pending pool. The supported `nodeDebug_warpL2TimeAtLeastBy(60)` API serialized a clock advance past that slot, and the existing pending transaction then mined. The executor had remained waiting; no new transaction was created or resent. Subsequent deployment checkpoints advanced normally. This was a local test-clock recovery operation, generated no proof, and did not reset or replace the L1 contracts.
