# V14 build and client integration evidence

The complete maintained V14 source compiled into 20 admitted contracts with all447 original APIs, all14 cached private-circuit identities and the19 dependent class identities checked. Its349 final source files match the native build exactly. This package records integration checks; the separate V14 fee/runtime packages record actual execution.

The prebuilt archive contains all370 authenticated members:20 native artifacts,349 final build inputs and the native manifest. Extraction and checked installation into a fresh final source copy passed without compilation, wallets, proofs or new keys. The corrected publisher accepts a complete original input snapshot or a complete final snapshot and rejects mixed bindings.

Actual SDK5.0.1 generation, copy and client sync passed for all20 contracts/40 files. The strict application typecheck passed against the17 original exported interfaces and the identical402 application/workspace source files, with zero fallback to the primary checkout's old contract wrappers. The normal deployment typecheck also passed. The temporary client config explicitly resolves the same installed Vite types; it does not relax application checks.

The pinned SDK accepts native artifacts without aztec_version at runtime and supplies its legacy version sentinel, but its generated TypeScript assertions require the property. The shared checked adapter changes only the two precise SDK assertions through unknown. All20 actual wrappers emit byte-identical JavaScript, preserve the SDK version sentinel and retain the same native bytes and actual class IDs. It does not inject metadata or change contract functionality.

The original native build used tooling4f69cca1; the installer correction used ac403fbc; final interface compatibility uses e7bd07a4. Their provenance is kept separate. The final adapter uses ES2020-compatible split/join. Source maintenance checks and patch replay precede the separately recorded installer/interface changes. There was no new Noir compilation for either tooling fix.

The checked client sync used public addresses from the local V14 fixture. Those addresses are not a production deployment and must not replace the existing client deployment index when promoting source. The release workflow generates the index from the integrator's chosen fresh deployment.

The source remains isolated pending final fee/runtime acceptance and explicit local promotion. No proof generation, new verification keys or network transactions were needed for these integration checks. No Vite bundle or browser runtime was claimed. Initial compiler-path, temporary type-resolution and SDK type-metadata failures remain recorded in summary.json; the unmodified base contracts tsconfig also has diagnostics outside the normal integration script.
